[tool.poetry]
name = "hierarchical_data"
version = "0.1.0"
description = ""
authors = ["ahasan09 <abulhasanraju@gmail.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
django = "^4.2"
django-mptt = ">=0.16,<0.19"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
